# Core
numpy>=1.24

# Prolog integration
janus_swi>=1.5

# Popper ILP (install from GitHub -- PyPI version is outdated)
# Popper's own dependencies (clingo, bitarray, python-sat) are resolved
# automatically by pip when installing from the GitHub source.
popper-ilp @ git+https://github.com/logic-and-learning-lab/Popper@main

# Jupyter kernel
ipykernel>=6.0
jupyter>=1.0
notebook>=7.0

# Development
pytest>=7.0
pytest-cov>=4.0
ruff>=0.4
mypy>=1.0
